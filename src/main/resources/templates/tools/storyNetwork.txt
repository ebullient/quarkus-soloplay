=== STORY NETWORK ===

Characters ({characters.size()}):
{#for c in characters}
- {c.name}{#if c.summary} ({c.summary}){/if}
{/for}

Locations ({locations.size()}):
{#for l in locations}
- {l.name}{#if l.summary} ({l.summary}){/if}
{/for}
{#if !recentEvents.isEmpty()}

Recent Events:
{#for e in recentEvents}
- Day {e.day}: {e.title}
{/for}
{/if}
