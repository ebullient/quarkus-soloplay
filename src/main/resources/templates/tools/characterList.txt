Characters in campaign:

{#for character in characters}
{character.name}{#if character.tags} [{#each character.tags}{it}{#if it_hasNext}, {/if}{/each}]{/if}{#if character.summary} - {character.summary}{/if} - ID: {character.id}
{/for}